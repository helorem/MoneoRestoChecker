CREATE TABLE transaction (
      id INTEGER PRIMARY KEY,
      name TEXT,
      amount INTEGER,
      datetime DATE
   );

CREATE TABLE balance (
      id INTEGER PRIMARY KEY,
      datetime DATE
      amount INTEGER,
      datetime DATE
   );


